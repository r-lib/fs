PKG_LIBS = $(shell pkg-config --libs libuv)
PKG_CPPFLAGS = -I./$(LIBUV)/include -D_WIN32_WINNT=0x0600 -DSTRICT_R_HEADERS -I.
CFLAGS += -D_WIN32_WINNT=0x0600

SOURCES = $(wildcard *.cc windows/*.cc)
OBJECTS = $(SOURCES:.cc=.o)
