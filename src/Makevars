UNAME := $(shell uname)

PKG_LIBS = $(shell pkg-config --libs libuv) -lbsd -pthread

SOURCES = $(wildcard *.cc unix/*.cc)
OBJECTS = $(SOURCES:.cc=.o)

PKG_CFLAGS = $(C_VISIBILITY)
PKG_CPPFLAGS = $(shell pkg-config --cflags libuv) -I. -pthread

